"""Scheduled jobs — APScheduler integration in Week 3."""

from app.config import settings


def schedule_drift_checks(scheduler) -> None:
    """Register drift detection job with APScheduler."""
    pass
